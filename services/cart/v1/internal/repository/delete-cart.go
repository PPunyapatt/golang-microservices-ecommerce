package repository

import (
	"context"
)

func (repo *Repository) DeleteCart(ctx context.Context, cartID string) error {
	// coll := repo.MongoDB.Database("ecommerce").Collection("cart")

	return nil
}
