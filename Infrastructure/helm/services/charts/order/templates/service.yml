apiVersion: v1
kind: Service
metadata:
  name: order-svc-helm
  namespace: {{ .Release.Namespace }}
  labels:
    app: order-service
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      name: grpc
    - port: 2112
      name: prometheus-port
  selector:
    app: order-app