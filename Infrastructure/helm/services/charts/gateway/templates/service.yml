apiVersion: v1
kind: Service
metadata:
  name: gateway-svc-helm
  namespace: {{ .Release.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  selector:
    app: gateway-app