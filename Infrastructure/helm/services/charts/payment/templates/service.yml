apiVersion: v1
kind: Service
metadata:
  name: payment-svc-helm
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      name: grpc
  selector:
    app: payment-app