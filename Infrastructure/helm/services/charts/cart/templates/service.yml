apiVersion: v1
kind: Service
metadata:
  name: cart-svc-helm
  namespace: {{ .Release.namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port}}
      name: grpc
  selector:
    app: cart-app