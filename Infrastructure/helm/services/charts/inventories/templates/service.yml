apiVersion: v1
kind: Service
metadata:
  name: inventory-svc-helm
  namespace: {{ .Release.Namespace }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      name: grpc
  selector:
    app: inventory-app